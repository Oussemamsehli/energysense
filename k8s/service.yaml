apiVersion: v1
kind: Service
metadata:
  name: energysense-core-api
  namespace: default
spec:
  type: NodePort
  selector:
    app: energysense-core-api
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080
